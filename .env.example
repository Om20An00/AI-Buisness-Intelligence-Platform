# Optional. Leave blank/unset to run InsightPilot entirely offline —
# the AI Explanation layer falls back to a deterministic, rule-based
# insight generator with zero external cost or dependency.
#
# Set this to enable real LLM-generated natural-language explanations.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
